<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="edu.eci.pdsw.persistence.mybatisimpl.mappers.CommentMapper">

    <resultMap type='Comment' id='CommentResult'>
        <id property='id' column='id'/>        
        <result property='comment' column='comment'/>
		<result property='date' column='date'/>
		<association property='user' javaType='User' resultMap='edu.eci.pdsw.persistence.mybatisimpl.mappers.UserMapper.UserResult'></association>
    </resultMap>
    <!--  
    <insert id="addComment" parameterType="Comment">
		INSERT INTO "comment"
		(id, comment, user_id,date) 
		VALUES((select count(*)+1 from "comment"),#{comment},#{idUser},#{date})
	</insert>
    -->
    
    <select id="loadComments" resultMap="CommentResult" parameterType="map">
        SELECT c.id,
        	c.comment,
			c.date,
			c.user_id,
			u.id idus,
			u.name,
			u.email,
			u.Type_user tuser
		FROM Comment as c , "User" as u where c.user_id=u.id;
    </select>
    
     <select id="loadComment" resultMap="CommentResult" parameterType="map">
        SELECT c.id,
        	c.comment,
			c.date,
			c.user_id,
			u.id idus,
			u.name,
			u.email,
			u.Type_user tuser
		FROM "comment" as c, "comment_initiative" as ci, "User" as u WHERE ci.initiative_id=#{id} and ci.comment_id=c.id and c.user_id=u.id;
	 </select>

</mapper>